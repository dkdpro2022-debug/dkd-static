export interface VideoItem {
  title: string;
  category: string;
  image: string;
  href: string;
}

export interface StoryItem {
  title: string;
  text: string;
  image: string;
}

export interface QuoteItem {
  name: string;
  role: string;
  avatar: string;
  text: string;
}
